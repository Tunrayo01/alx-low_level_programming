#include <stdio.h>

/**
 * main - putchar function
 *
 * Return: always 0(success)
 */

int main(void)
{
	char j;
	char k;

	for (j = 'a'; j <= 'z'; j++)
		putchar(j);
	for (k = 'A'; k <= 'Z'; k++)
		putchar(k);
	putchar('\n');
	return (0);

}
